apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: simple-web-app-hpa
  namespace: sre-challenge
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: simple-web-app # The HPA targets this Deployment for scaling
  minReplicas: 2
  maxReplicas: 4
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 50
