apiVersion: v1
kind: Service
metadata:
  name: simple-web-app-service
  namespace: sre-challenge
spec:
  selector:
    app: simple-web
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
  type: ClusterIP
